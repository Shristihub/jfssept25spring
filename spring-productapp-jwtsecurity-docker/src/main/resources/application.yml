#datasource specific properties
spring:
  datasource:
    url: jdbc:mysql://sqlcontainer:3306/productkart #containername
    username: root
    password: root		

#jpa specific properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
jwt:
 secret: shristi
